---
import EsCatchAll from '../../[...path].astro';
const raw = Astro.params.path ?? '';
const normalizedPath = (`/es/docs/legal/${String(raw).replace(/^\/+|\/+$/g, '')}/`).replace(/\/{2,}/g, '/');
---

<EsCatchAll normalizedPath={normalizedPath} />
